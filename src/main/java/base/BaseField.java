package base;

import org.openqa.selenium.By;

public class BaseField extends BaseElement {
    public BaseField(By currentLocator, String elementName) {
        super(currentLocator, elementName);
    }
}
