package base;

import org.openqa.selenium.By;

public class BaseButton extends BaseElement {
    public BaseButton(By currentLocator, String currentName) {
        super(currentLocator, currentName);
    }
}
